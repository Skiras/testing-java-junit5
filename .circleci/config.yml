version: 2.1

orbs:
  maven: circleci/maven@0.0.12

workflows:
  maven_test:
    jobs:
      build:
        docker:
          - image: circleci/openjdk:11-browsers-legacy
        - run: mvn integration-test
